name: example
description: "Garmisch Design System Showcase App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Garmisch Design System
  garmisch:
    path: ../

  cupertino_icons: ^1.0.8
  flex_color_picker: ^3.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

